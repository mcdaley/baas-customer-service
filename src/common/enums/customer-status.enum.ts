//-----------------------------------------------------------------------------
// src/common/enums/customer-status.enum.ts
//-----------------------------------------------------------------------------

/**
 * Enum that defines all of the possible Customer statuses.
 * 
 * @enum CustomerStatus
 */
export enum CustomerStatus {
  Pending = 'Pending',
  Active  = 'Active',
  Blocked = 'Blocked',
}